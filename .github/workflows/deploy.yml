name: Deploy to Render

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Trigger Render Deploy
      run: |
        echo "Render will auto-deploy via webhook"
        echo "Make sure to connect your GitHub repo to Render"
